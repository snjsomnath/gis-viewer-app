import React from 'react';
import MapViewer from './components/MapViewer';

const App: React.FC = () => {
  return (
    <div id="app-container">
      <MapViewer />
    </div>
  );
};

export default App;